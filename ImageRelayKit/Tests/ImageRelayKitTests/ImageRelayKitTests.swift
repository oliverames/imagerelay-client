import Testing
@testable import ImageRelayKit

@Suite("ImageRelayKit Tests")
struct ImageRelayKitTests {
    @Test("Package placeholder")
    func placeholder() {
        // Placeholder test — replace with real tests as the package grows.
        #expect(true)
    }
}
